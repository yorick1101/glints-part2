Number of funds

db.getCollection("companies").aggregate([
{
    $project:{
       "name" : 1,
       "count":{
           "$size": { "$ifNull": [ "$fundingrounds", []]}
       }     
    }
},{
    $match:{
        count: { "$gt" : 0}   
    }
}])